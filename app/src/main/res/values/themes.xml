<resources>

    <style name="Base.Theme.FeelWell" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/background</item>
        <item name="colorPrimary">@color/background</item>
        <item name="colorAccent">@color/accent</item>

        <item name="android:textColor">@color/text</item>

        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowLightNavigationBar">true</item>

        <item name="android:textColorPrimary">@color/text</item>
        <item name="android:textColorSecondary">@color/text</item>

        <item name="preferenceCategoryTitleTextColor">@color/text</item>
        <item name="preferenceCategoryTitleTextAppearance">
            @style/TextAppearance.Material3.ActionBar.Title
        </item>

    </style>

    <style name="Theme.FeelWell" parent="Base.Theme.FeelWell" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="android:windowLightStatusBar">true</item>
        <item name="windowSplashScreenBackground">@color/background</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/backgroundLight</item>

        <item name="windowSplashScreenAnimationDuration">1500</item>
        <item name="android:windowActionBar">false</item>

        <item name="postSplashScreenTheme">@style/Base.Theme.FeelWell</item>
    </style>
</resources>