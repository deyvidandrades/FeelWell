<resources>

    <style name="Base.Theme.FeelWell" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/light_background</item>
        <item name="colorPrimary">@color/light_background</item>
        <item name="android:colorPrimary">@color/light_background</item>
        <item name="colorAccent">@color/light_accent</item>
        <item name="android:colorAccent">@color/light_accent</item>

        <item name="android:textColor">@color/light_text</item>

        <item name="android:statusBarColor">@color/light_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/light_background</item>
        <item name="android:windowLightNavigationBar">true</item>

        <item name="android:textColorPrimary">@color/light_text</item>
        <item name="android:textColorSecondary">@color/light_text</item>

        <item name="preferenceCategoryTitleTextColor">@color/light_text</item>
        <item name="preferenceCategoryTitleTextAppearance">
            @style/TextAppearance.Material3.ActionBar.Title
        </item>

    </style>

    <style name="Theme.FeelWell" parent="Base.Theme.FeelWell" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="android:windowLightStatusBar">true</item>
        <item name="windowSplashScreenBackground">@color/light_background</item>

        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/light_background</item>

        <item name="windowSplashScreenAnimationDuration">1500</item>
        <item name="android:windowActionBar">false</item>

        <item name="postSplashScreenTheme">@style/Base.Theme.FeelWell</item>
    </style>
</resources>